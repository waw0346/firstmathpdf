# 📚 크롭 오류 학습 기록 (LEARNING.md)
> 수학의 지름길 학원 LLM-Wiki — 반복 오류 방지를 위한 기록
> 마지막 업데이트: 2026-05-30

---

## 🔴 오류 #1: 좌/우 2단 컬럼 미분리 (해결됨)
**증상**: 이미지가 페이지 전체 너비(2199px)로 크롭되어 좌우 두 문제가 동시에 표시됨  
**원인**: `crop()` 시 `x_start=margin, x_end=w-margin` 으로 전체 너비 사용  
**해결**: PDF 텍스트 위치로 `mid_x = w_pt * 0.48` 탐지 후 L/R 컬럼 분리  
**적용**: `crop_pdf_full()` 함수, `crop_subj()` 함수

---

## 🔴 오류 #2: 선택과목 3과목이 동일 이미지 공유 (해결됨)
**증상**: 확통/미적분/기하 23-30번이 모두 같은 이미지를 가리킴  
**원인**: PDF 내 "23."이 처음 나오는 페이지(확통)만 탐지, 미적분/기하는 스킵됨  
**해결**: 과목별 시작 페이지 명시 `SUBJ_PAGES = {확통:(8,11), 미적분:(12,15), 기하:(16,19)}`  
**검증**: 2025/2026 수능 모두 동일 페이지 구조 확인  

---

## 🟠 오류 #3: 하단 선택지 잘림 (해결됨)
**증상**: ④⑤ 선택지 또는 x축 라벨이 이미지 하단에서 잘림  
**원인**: `trim()` 시 bottom padding(25px)이 부족, 선택지 사이 흰줄에서 조기 종료  
**해결**: `bot_pad=40`, `content threshold=0.008` (픽셀 0.8% 이상이면 내용으로 판정)  
**규칙**: 하단은 "마지막 내용 행 + 40px" 유지

---

## 🟠 오류 #4: 오른쪽 텍스트 잘림 (해결됨)
**증상**: 우컬럼 문제에서 수식/텍스트 오른쪽 끝이 잘림  
**원인**: `xe = w_pt - mg_r` 에서 `mg_r = w_pt * 0.018` (15pt) 이 너무 큼  
**해결**: 우측 여백 `mg_r = w_pt * 0.010` (8pt)로 축소  
**좌우 여백 규칙**: 좌=18pt, 우=8pt (비대칭 — 우측은 최소화)

---

## 🟠 오류 #5: 상단 구분선 제거 과함 (해결됨)
**증상**: trim 후 이미지가 너무 작아짐 (확통_023 = 148px)  
**원인**: `threshold=247` 으로 세팅 시 연한 회색 줄도 흰줄로 처리  
**해결**: 구분선 감지를 `df > 0.50` (50% 이상 검정 픽셀)으로 제한  
**규칙**: 가로 구분선 = 폭의 50% 이상이 진한(< 80) 픽셀

---

## 🟡 오류 #6: 2025 10월 모의고사 이미지 없음 (미해결)
**증상**: 10월 모의 22개 문제 이미지 없음 또는 너무 작음(111~142px)  
**원인**: PDF 파일 없음 (`sources/2025/` 에 10월 모의 PDF 미존재)  
**해결방법**: PDF 파일을 `sources/2025/2025_10월모의_수학문제.pdf` 경로에 추가 후  
  `python3 scripts/crop_problems.py --pdf 2025_10월모의_수학문제.pdf` 실행  
**현재상태**: ⏳ PDF 파일 대기 중

---

## 🟡 오류 #7: 미적분_ vs 미적_  파일명 혼용 (해결됨)
**증상**: DB ID는 `미적_` 이지만 crop 파일은 `미적분_` 으로 저장됨  
**원인**: `crop_subj()` 에서 subj='미적분' 으로 파일명 생성, DB는 기존 `미적_` ID 유지  
**해결**: DB crop_path를 `미적분_` 파일 경로로 업데이트  
**확인**: `2025_수능_고3_미적_023` → `sources/crops/2025_수능_고3_미적분_023.jpg` ✅

---

## 📐 크롭 파라미터 최적값 (2026-05-30 현재)

```python
# crop_pdf_full / crop_subj 공통 설정
dpi      = 200          # 출력 해상도
mid_x    = w_pt * 0.48  # 중앙 구분선 (페이지의 48% 지점)
mg_l     = w_pt * 0.018 # 좌측 여백 (≈15pt)
mg_r     = w_pt * 0.010 # 우측 여백 (≈8pt, 비대칭)
y_top    = problem_y - 15  # 문제 번호 위 15pt
y_bot    = next_problem_y - 4  # 다음 문제 바로 위

# smart_trim 설정
top_pad  = 10   # 상단 트림 후 여백
bot_pad  = 40   # 하단 내용 끝 + 여백 (선택지 잘림 방지)
white_th = 247  # 흰줄 판정 (mean > 247)
rule_th  = 0.50 # 구분선 판정 (진한픽셀 비율 > 50%)
content_th = 0.008  # 내용 픽셀 판정 (< 180인 픽셀이 0.8% 이상)
```

---

## 📅 크롭 재실행 방법

```bash
# 전체 재크롭 (수능 공통 + 선택과목)
cd C:\Users\oem\Documents\math_llm
python3 scripts/crop_all.py

# 특정 PDF만
python3 scripts/crop_all.py --year 2026

# 크롭 후 DB/Obsidian 동기화
python3 scripts/obsidian_sync.py
python3 scripts/verify.py
```

---

## ✅ 체크리스트 (크롭 후 반드시 확인)

- [ ] 전체 너비(>1500px) 이미지 없음
- [ ] 너무 작은(<150px) 이미지 없음  
- [ ] 선택지 ①②③④⑤ 모두 표시됨
- [ ] 그림/도형 하단 x축 표시됨
- [ ] 좌우 텍스트 잘림 없음
- [ ] 선택과목 3과목 이미지 다름 (확인: 확통≠미적분≠기하)
---

## 🟠 오류 #8: 10월 모의고사 L컬럼 오른쪽 잘림 (해결됨)
**증상**: L컬럼 문제에서 `[3점]`, 수식 끝부분이 오른쪽에서 잘림  
**원인**: 10월 모의고사 L컬럼 텍스트가 mid_x까지 뻗음 (xe=388pt 이지만 텍스트는 412pt까지)  
**해결**: L컬럼 xe = `mid_x - mg_l*0.3` (≈392pt → ≈397pt로 확장)  
**규칙**: 10월 모의고사는 수능보다 텍스트 밀도가 높음, L컬럼 경계를 mid_x에 더 가깝게 설정

---

## ✅ 크롭 최종 확정 파라미터 (v5, 2026-06-02)

```python
# 올바른 컬럼별 xe 계산 방법
dpi     = 250              # 200→250 고해상도 (기호/수식 선명)
HEADER_Y = 160             # 페이지 상단 헤더 제외 y 기준
mid_x   = w_pt * 0.48     # 컬럼 구분선

# L컬럼 xe: 같은 컬럼(bx0 < mid_x) 블록 최대 x1 + 20pt
# R컬럼 xe: 같은 컬럼(bx0 >= mid_x) 블록 최대 x1 + 20pt
# ★ bx0 < mid_x (strict, +30 아님) — R컬럼 텍스트 혼입 방지

# Obsidian 표시 너비
img_width = 700  # 기본, 세로>2000: 750, 세로<800: 650
# ![[파일명|700]] 형식으로 삽입
```

**오류 #9: L컬럼 xe 계산 시 R컬럼 블록 혼입**
- 원인: `bx0 < mid_x + 30` 조건이 R컬럼 블록(x0=429)을 포함
- 해결: `bx0 < mid_x` (엄격한 경계)로 수정
- 결과: L컬럼 ~1180-1338px, R컬럼 ~1182-1185px (정상)

---

## ✅ 크롭 최종 확정 파라미터 v6 (2026-06-02)

```python
# 핵심: L컬럼 xe = mid_x + 60pt (고정)
# 이유: 표/수식이 컬럼 경계 넘어가도 완전 포함 보장
# smart_trim이 실제 내용 경계만 남겨줌

dpi      = 250
mid_x    = w_pt * 0.48
mg_l     = w_pt * 0.015
HEADER_Y = 160        # 페이지 헤더 제외

# L컬럼
xs_L = mg_l
xe_L = mid_x + 60    # ← 핵심! 표 우측 여유 60pt

# R컬럼  
xs_R = mid_x + mg_l
xe_R = w_pt - 3      # 페이지 끝까지
```

**오류 #10**: L컬럼 xe 동적계산 시 필터 오류들
- bx0+30 → R컬럼 포함 (오류 #9)
- bx0<mid_x strict → 표 오른쪽 컬럼 제외 (이번 오류)
- **최종해결**: 동적계산 포기, mid_x+60 고정값 사용 + smart_trim 의존

---

## ✅ 크롭 최종 확정 파라미터 v10 (2026-06-02)

```python
dpi=250; HEADER_Y=160
L: xs=mg_l, xe=mid_x+60 → trim_smart_v10(is_L=True)
R: xs=mid_x+mg_l, xe=w_pt-3 → trim_smart_v10(is_L=False)

def find_and_mask_vline(g):
    # 오른쪽 12% 영역에서 열 60% 이상 어두우면 수직선 → 흰색 마스킹

def trim_smart_v10(img, is_L):
    top: 구분선/흰줄 제거
    last: 수직선 마스킹 후 전체 이미지로 하단 탐지 (left-85% 아님!)
    right(is_L만): 흰공백 ≥50px 탐지로 R컬럼 블리드인 제거
```

---

## ✅ 최종 확정 파라미터 v19 (2026-06-03 09:43)

### crop_all.py 핵심 설정값
```python
DPI            = 250    # 해상도 (기호·수식 선명)
HEADER_Y       = 160    # 페이지 헤더 제외 기준 (pt)
FOOTER_Y       = 1075   # 하단 제외 기준 (페이지번호박스=1082pt)
DRAWING_RANGE  = 200    # 텍스트 끝 이후 도형 포함 범위 (pt)
MID_RATIO      = 0.48   # 좌/우 컬럼 구분선 비율
MG_L_RATIO     = 0.015  # 좌측 여백 비율
XE_L_EXTRA     = 60     # L컬럼 우측 여유 (pt, 정규분포표 포함용)
CLIP_TOP_EXTRA = 5      # 클립 상단 여유 (pt, 이전문제 블리드 방지)
TOP_PAD        = 25     # 상단 트림 후 여백 (px)
BOT_PAD        = 40     # 하단 마지막 내용 후 여백 (px)
```

### 핵심 알고리즘 (오류 이력)
| 오류 | 원인 | 해결 |
|------|------|------|
| 2단 컬럼 미분리 | 전체 너비 크롭 | mid_x=0.48 L/R 분리 |
| 선택과목 동일이미지 | 첫 23번만 탐지 | 과목별 페이지범위 고정 |
| 상단 이전문제 블리드 | clip y0-25pt | **y0-5pt로 축소** |
| 하단 빈공간 | 픽셀스캔 오류 | **PDF 텍스트 좌표 기반** |
| R컬럼 블리드인 | 화이트갭 미탐지 | **cr=min(갭,mid_x_px)** |
| 확인사항 포함 | admin_y=1140pt | **동적 admin_y 감지** |
| 답안지 그리드 | 도형 무제한 포함 | **draw_limit=last_text+200pt** |
| L컬럼 텍스트 오염 | x 필터 없음 | **is_L and bx0>mid_x 제외** |
| R컬럼 내용 소실 | x 필터 전체 적용 | **is_L 플래그로 L컬럼만** |
| 페이지번호 박스 | FOOTER_Y=1140 | **FOOTER_Y=1075** |
| VirtioFS 갱신 실패 | 직접 덮어쓰기 | **/tmp 저장 후 shutil.copy** |

### 선택과목 페이지 구조 (수능/모의 공통)
```
확통:   0-based p8~11
미적분: 0-based p12~15
기하:   0-based p16~19
```

### 실행 방법
```bash
# 전체 크롭
python3 scripts/crop_all.py

# 특정 연도만
python3 scripts/crop_all.py 2026

# 특정 연도+시험
python3 scripts/crop_all.py 2025 수능

# 크롭 후 Obsidian 동기화
python3 scripts/obsidian_sync.py
```

---

## 🔴 오류 #11: 클립 경계 블리드 텍스트 (2026-06-04 해결)

**증상**: 5번처럼 짧은 문제에 1575px 같은 과도한 빈공간
**원인**: PDF text block이 clip.y1 경계를 2-8pt 초과해 감지됨
  예) 5번 clip.y1=610pt인데 6번 텍스트가 y1=618pt로 감지
**해결**: `if by1 > clip.y1 + 5: continue` 필터 추가
  도형도 동일: `if r.y1 > clip.y1 + 5: continue`
**결과**: 5번 1575px → **208px** ✅

---

## 🔴 오류 #12: 지수·분수 위첨자 상단 잘림 (2026-06-04 해결)

**증상**: `y=(1/5)^x` 처럼 지수가 있는 문제에서 지수 윗부분이 잘림
**원인**: "20." span_y0=168.5pt인데 지수 스팬은 y0=158.8pt → 9.7pt 위에 있음
  현재 clip_y0 = span_y0-5 = 163.5pt → 지수 4.7pt 잘림
**해결**: 문제번호 주변 15pt 이내의 모든 스팬 y0 스캔 → effective_y0 = min값 사용
  effective_y0 = 158.8pt → clip_y0 = 153.8pt → 지수 완전 포함
**코드**: get_problem_positions() 내 effective_y0 계산 로직 추가

---

## 🔴 오류 #13: 수직선 안티앨리어싱 잔존 (2026-06-04 해결)

**증상**: 이미지 좌측 x=4~20px에 희미한 수직선 잔존
**원인**: 마스킹 임계값 <80이 너무 엄격 → 안티앨리어싱 픽셀(80~128) 미처리
**해결**: 임계값 <80 → **<150**, 비율 >0.45 → **>0.30**으로 완화
  - mask_vlines(), crop_and_save() 내 마스킹 모두 수정
**최종**: 전체 164개 이미지 수직선 0개

---

## ✅ 파일 명명 체계 v4.0 최종 확정 (2026-06-05)

### 설계 원칙
1. 지역=영문2자리, 학교=한글(원래 이름)
2. 수능은 SU (Korean Official → SU로 변경, KO=올림피아드)
3. 교육과정 완전 반영: P1/P2(공통1/2), A(대수), C1/C2(미적분Ⅰ/Ⅱ), SS(확통), G(기하)
4. 파일 단위 10-11자리, 항목 단위 12자리
5. 해답=_A, 해설=_S 접미사

### 경시대회·출판사 코드 추가
ST=성대경시 KC=KMC KO=KMO IO=IMO
EB IT MG JS BL RP SE HA (출판사)

### 학생 시스템 DB 테이블
students → study_sessions → student_answers 순으로 연결
